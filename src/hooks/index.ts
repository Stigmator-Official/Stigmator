export { useReducedMotion, useAnimationConfig } from "./use-reduced-motion";

// Admin & Permission hooks
export {
  useAdmin,
  usePermission,
  useRole,
} from "./useAdmin";

// Re-export permission types
export type {
  UserRole,
  Permission,
} from "@/lib/permissions";
